import '../status_folder_permission_screen.dart';

class WhatsAppStatusFolderPermission extends StatusFolderPermissionScreen {
  const WhatsAppStatusFolderPermission({super.key})
    : super(isBusinessMode: false);
}
